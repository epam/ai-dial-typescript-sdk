name: Release Workflow

on:
  push:
    branches: [development, release-*]
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    uses: epam/ai-dial-ci/.github/workflows/node_release.yml@test-trusted-publishing
    with:
      promote: true # HACK: skip RC phase, project does not require stabilization period before releasing stable versions
      docker-build-enabled: false
    secrets: inherit
